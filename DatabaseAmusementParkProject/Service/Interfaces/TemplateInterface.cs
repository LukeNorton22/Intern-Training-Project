namespace DatabaseAmusementParkProject.Service.Interfaces
{
    // 📁 This namespace is typically used for storing interfaces for service classes.
    // Interfaces define *what* a service should do, but not *how* it does it.
    // The actual implementation lives in a class like TemplateService.cs.

    // 🧠 What is an Interface?
    // An interface is like a contract — it defines the methods a class *must* implement.
    // This allows us to write code that depends on abstractions, not concrete classes,
    // which makes our code more testable, flexible, and maintainable.

    public interface ITemplateInterface
    {
        // 🚧 You will define method signatures here — no logic or code!
        // Example:
        // Task<TemplateDto> GetTemplateByIdAsync(int id);
    }
}
